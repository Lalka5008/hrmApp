<Window x:Class="hrm.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:hrm"
        xmlns:vm="clr-namespace:hrm.ViewModels"
        xmlns:v="clr-namespace:hrm.Views"
        Title="HR Management System" Height="600" Width="800">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:DepartmentViewModel}">
            <v:DepartmentView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:EmployeeViewModel}">
            <v:EmployeeView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Navigation Panel -->
        <StackPanel Grid.Column="0" Background="#FFE3E3E3">
            <Button Content="Departments" Command="{Binding ShowDepartmentsCommand}" Margin="5"/>
            <Button Content="Employees" Command="{Binding ShowEmployeesCommand}" Margin="5"/>
        </StackPanel>

        <!-- Content Area -->
        <ContentControl Grid.Column="1" Content="{Binding CurrentViewModel}"/>
    </Grid>
</Window>